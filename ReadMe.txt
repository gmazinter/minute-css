notes:
1) i chose to re-start the video on every re-mount ('onmouseenter') beacuse it was as easy as recreating the video element. 
1) i chose to re-start the video on every re-mount ('onmouseenter') beacuse it was as easy as recreating the video element. 
1) i chose to re-start the video on every re-mount ('onmouseenter') beacuse it was as easy as recreating the video element. 